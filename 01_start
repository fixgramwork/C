#include <stdio.h>

int main() {
	// 변수(Variable)
	// DataType 변수명;   항상명령뒤에는 ;(세미콜론)을 붙여야 합니다. 
	//               명령이 여기까지야 라는 것을 알려줍니다.
	// 변수명은 영대소문자, _, 숫자, 숫자는 첫글자로 사용이 안됩니다.
	// 변수명의 결이는 표준 C는 31문자, visualstduio 128 문자
	// 변수명의 명명 규칙은 저장하는 값의 용도를 알려주는 단어나 문장으로 표현하시면 됩니다.
	int score;   // 변수는 선언과 동시에 정의한다.
	int isLive;

	int a;
	score = 20;

	// 같은 블럭안에는 동일한 변수명이 있을 수 없습니다.


	return 0;
}
